"""Nav: upper-level operator UI.

Composes the world_map fuser (map views + pose fusion) and the chassis
controller (Pi driver + teleop) in one Qt process. This is the shell
where SLAM primitives, goal commands, and mission-level buttons will
eventually live.
"""
